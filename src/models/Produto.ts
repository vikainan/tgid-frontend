export type Produto = {
    id: number;
    nome: string;
    descricao: string;
    preco: number;
    categoria_id: number;
    foto: string;
    resumo: string;
    quantidade: number
};
